<link rel="stylesheet" href="/static/vendor/datatables/datatables.min.css" />
<%- include('../partials/header.admin.ejs'); %>
<div class="container-fluid">
  
  <div class="row container-fluid">
    <div class="col-lg-12">
      <div class="card shadow mb-4">
        <div class="card-header py-3">
          <a href="/admin/cursos/<%= programa%>" class="btn btn-success btn-sm float-left d-inline"><i class="fas fa-arrow-left"></i></a>

        </div>
        <div class="card-body">
            <%if(!datos.length) {%> 
              <h5>No existen cursos Finalizados</h5>              
            <%}else{%> 
            <h5>Cursos finalizados:</h5>
            <% } %>
            <div class="card shadow mb-3">
              <div class="card-body">
                <table id="datatable" class="table table-bordered" width="100%" cellspacing="0">
                  <thead>
                    <tr>
                      <th>Código de curso</th>
                      <th>Nombre</th>
                      <th>Instructor</th>
                      <th>Orden de inicio</th>
                      <th>Agrupación</th>
                      <th>Ver</th>
                    </tr>
                  </thead>
                  <tbody>
                      <% for(var i=0; i<datos.length; i++) {%>
                        <tr>
                            <td><%= datos[i].Codigo_curso %> </td>
                            <td><%= datos[i].Nombre %> </td>
                            <td><%= datos[i].instructor %></td>
                            <td><%= datos[i].Orden %></td>
                            <td><%= datos[i].Agrupacion %></td>
                            <td>
                              <a  href="/admin/cursos/detallectz/<%= datos[i].Codigo_curso %>/<%= programa%>/oferta" class="btn btn-sm btn-info">Ver</a></td>
                        </tr>
                      <% } %>
                  </tbody>
                </table>
        </div>
      </div>
    </div>
  </div>
</div>
<!-- /.container-fluid -->
<%- include('../partials/footer'); %>
        <!-- Page level plugins -->
        <script src="/static/vendor/datatables/datatables.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>
        <!-- Page level custom scripts -->
        <script>
           $("#datatable").DataTable()
        </script>